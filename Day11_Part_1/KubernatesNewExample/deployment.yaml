apiVersion: apps/v1
kind: Deployment

metadata:
  name: kubernatesexample-deployment

spec:
  replicas: 2

  selector:
    matchLabels:
      app: kubernatesexample-newapp

  template:
    metadata:
      labels:
        app: kubernatesexample-newapp

    spec:
      containers:
      - name: spring-container
        image: kubernates-newdemo
        imagePullPolicy: Never

        ports:
        - containerPort: 3333