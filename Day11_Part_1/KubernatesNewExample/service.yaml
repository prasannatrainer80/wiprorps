apiVersion: v1
kind: Service

metadata:
  name: springboot-service

spec:
  selector:
    app: kubernatesexample-newapp

  ports:
  - protocol: TCP
    port: 3333
    targetPort: 3333
    nodePort: 31122

  type: NodePort