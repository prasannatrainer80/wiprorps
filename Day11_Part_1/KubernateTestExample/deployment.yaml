apiVersion: apps/v1
kind: Deployment

metadata:
  name: kubernatesexample-deployment

spec:
  replicas: 2

  selector:
    matchLabels:
      app: kubernatesexample-testapp

  template:
    metadata:
      labels:
        app: kubernatesexample-testapp

    spec:
      containers:
      - name: spring-container
        image: kubernatesexample-testapp
        imagePullPolicy: Never

        ports:
        - containerPort: 2222